#include<stdio.h>
int main()
{
int a,b,c;
printf("enter marks in sec 1,2,3=");
scanf("%d%d%d,"&a,&b,&c);
sum=a+b+c;
if(sum>100&&a>10&&b>10&&c>10);
printf("chef passed thge exam");
else
printf("chef failed the exam");
return 0;
}
